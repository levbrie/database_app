<%= form_for @artwork, :html => { :class => 'form-horizontal'}  do |f| %>  
<% if @artwork.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artwork.errors.count, "error") %> prohibited this artwork from being saved:</h2>

      <ul>
      <% @artwork.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :art_id %>
    <%= f.number_field :art_id %>
  </div>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :value %>
    <%= f.number_field :value %>
  </div>
  <div class="field">
    <%= f.label :condition %>
    <%= f.text_field :condition %>
  </div>
  <div class="field">
    <%= f.label :collection_id %>
    <%= f.number_field :collection_id %>
    <%#= select_tag "Collection ID", options_from_collection_for_select(Collection.all, "collection_id", "collection_id") %>
  </div>
  <div class="field">
    <%= f.label :purchase_date %>
    <%= f.date_select :purchase_date %>
  </div>
  <div class="field">
    <%= f.label :creation_date %>
    <%= f.date_select :creation_date %>
  </div>

<hr>
  <h2>Physical Characteristics</h2>
<%= f.fields_for :physical_characteristic do |builder| %>
  <%= render 'physical_characteristic_fields', f: builder %>
<% end %>
<%#= link_to_add_fields "Add Physical Characteristics", f, :physical_characteristic %>




  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
  </div>
<% end %>
